def increment(n: int) -> int: #this species the finction recieves the int value and return the int value...
    return n+1

count: int = 0
# so here using the "int" Annotation we r speacifing the count takes the int value...python ignores the annotations and runs the code...

print(increment(count))